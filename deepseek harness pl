你是本插件项目的实施代理。仓库根目录的 SPEC-v1.0-R1.md 是唯一权威规格书，
请先完整阅读再开工。

执行规则：
1. 从 P0 开始，严格按各阶段出口条件推进，未通过出口条件不得进入下一阶段
2. 规格书标注"须自行验证"的事项，一律用工具读取源码/运行命令获取一手信息，
   禁止凭训练记忆假设
3. 到达 P0 数据通道决策门与 P3 发消息决策门时：展示探针证据
   （命令输出/console 输出）并给出建议结论，暂停等待我确认后继续
4. 规格书与实际源码/行为冲突时，以源码为准，并在 SPEC-DEVIATIONS.md
   中逐条记录偏差（含出处）
5. §8"人工验收"各项不可自动化：实现至该处即标记 TODO-human 并继续其余工作
6. 浏览器内验证优先用无头浏览器（Playwright 等）采集 DOM 与 console 输出；
   无法自动化时，向我报告需人工观察的具体点位，不要猜测结果

RhineLabUI × dsh 插件整合规格书
文档版本：v1.0-R1（重大修订版）
基于 dsh 锚定版本：v0.1.2-rc.1（npm dist-tag `latest`；本机已装并在服务 127.0.0.1:3080；2026-09-03 发布）【v1.0-R2 修正：原锚定 v0.1.5-alpha.1，实测其为 `alpha` tag 而非 `latest`】
修订日期：2026-09-09（v1.0-R1）；2026-09-09（v1.0-R2 实证修订）
取代文档：v0.9 草案（原始计划书 + 首版代理规格书）
变更级别：重大（架构双通道 + 风险前置 + 版本锚定更新）
本次修订的触发源有两个：① 对 v0.9 草案的落地性审查，识别出 11 项问题（A 级 3 项、B 级 5 项、C 级 4 项）；② dsh 官方发布节奏核查——v0.1.5-alpha.1 于昨日（2026-09-08）发布，包含会话格式 V3、Agent API 破坏性调整（移除 ctx.agent）、Inbox API 变更（agent.inbox 替代公共类）等直接影响插件开发的改动。这两点共同决定了 v1.0 的两个核心动作：把最大未知数（数据通道）从第 3 周前置到第 1 周探针；起步锚定版本从 open-sea-skin 实测的 v0.1.2-alpha.3 升级为 v0.1.5-alpha.1。
0. 版本演进与文档控制
文档版本	日期	性质	核心变更	
v0.9 草案	2026-09-08 前	原始计划	用户原始计划书 + 首版代理规格书（混用两条未验证路径）	
v1.0-R1	2026-09-09	重大修订	① 架构改为"视觉 DOM 路径 + 数据双通道"分离；② P0 重构为三探针实验；③ 锚定 dsh v0.1.5-alpha.1；④ 11 项审查修订全部落款	
v1.0-R2	2026-09-09	实证修订	① 锚定改 v0.1.2-rc.1（R-9 修正）；② 通道 C 重定义为"独立兜底 D：宿主半直读会话日志"（R-2 修正）；③ 视觉层"照抄"边界收敛为管线（R-3 补注）；④ 新增 R-12~R-16 五项风险；⑤ 出处与实证见 SPEC-DEVIATIONS.md	
v1.0-R1 摘要：视觉接管照抄 open-sea-skin 的纯 DOM 路径（唯一实证方案）；数据获取拆为"官方通道（ctx services）"与"同源直连通道（fetch/WebSocket）"双路，由 P0 探针实测决定主用通道；发消息通道在 P3 前置决策门；新增 v0.1.5 专项核对（ctx.agent 移除、Inbox 变更、会话 V3 格式对数据桥的影响）。
v1.0-R2 摘要（以源码为准，实证过程见 SPEC-DEVIATIONS.md）：① 锚定 0.1.2-rc.1——实测 npm dist-tags 为 `latest`=0.1.2-rc.1 / `alpha`=0.1.5-alpha.1，本机所装即 0.1.2-rc.1；已核对 0.1.2-rc.1 与 0.1.5-alpha.1 在本插件全部依赖面（session/* RPC、ctx.remote、/api/remote.mux、shell.overlay）零差异，故锚定风险已消除。② 后端不存在 REST 会话端点，全部数据只经 `/api` RPC 与 `/api/remote.mux`（Typert Remote），通道 B 与通道 C 实为同一传输，决策门"二选一"不成立；改为"B 主用 + 独立兜底 D（宿主半直读 `$DSH_HOME/sessions/**/session.jsonl.zstd`）"。③ open-sea-skin 是背景+玻璃皮肤而非全屏替换，可照抄的是管线（bundle 门面、webServer 静态路由、`<style>` 注入），不是视觉架构；其更新的 `harness-plugin/` 路径依赖上游不存在的 `shell.background` 槽位，禁止采用。④ 规格书原"v0.1.5 专项核对"中，会话格式 V3 仅在采用兜底 D 时成为 P2 前置；ctx.agent 与 Inbox 属宿主侧，与本客户端插件无交集。
1. 目标与硬约束（含 R-9 修正）
目标：将 RhineLabUI 封装为 dsh 客户端 UI 插件 dsh-plugin-rhinelab-ui，实现"启用刷新→3D 档案室 / 禁用刷新→原生界面"的无损切换，交付计划书第九节描述的完整体验。
硬约束（违反任意一条即返工）：
不修改 dsh 源码任何一行——所有改动封装在插件仓库内，经 cordis.patch.yml 的 insert 挂载【R-原文】。
不跨包导入 dsh 内部符号——插件间/插件对内核的引用只走 slot 系统与 ctx services【R-原文】。
卸载即还原——原生 UI 压制仅通过插件运行时注入的 <style> 标签实现；localStorage 键一律 rhinelab-ui: 前缀，用户数据（收藏、会话）属合法持久痕迹不清理【R-11】。
资产全部本地化——无 CDN 请求、无数据收集【R-原文】。
版本锚定策略【R-9 修正，v1.0-R2】——起步锁 v0.1.2-rc.1（npm `latest`，2026-09-03 发布，本机已装并在服务 127.0.0.1:3080）；README 声明"仅测试通过此版本"；升级流程：全链路跑通 → 升级 dsh → 回归探针 B → 更新锚定声明。修正依据：实测 `npm view @deepseek-ai/dsh dist-tags` 为 `latest`=0.1.2-rc.1、`alpha`=0.1.5-alpha.1、`next`=0.1.2-rc.1，原锚定的 v0.1.5-alpha.1 是 alpha 预发布而非最新稳定版；且已核对两版在本插件全部依赖面（`session/*` RPC 方法集、`ctx.remote`、`/api/remote.mux`、`shell.overlay`）逐一相同，先锚已装版本可零环境变更开工。v0.1.5 的长会话卡顿/Windows 兼容改进作为**升级目标**保留，不再作为起步前提（出处：SPEC-DEVIATIONS.md D-1）。
2. 参考资源清单
资源	地址	用途	
模板仓库	https://github.com/d-dev0101/open-sea-skin	视觉接管路径唯一抄写来源：plugin/client.js 全文、cordis.patch.yml、多端 CSS 兼容写法、性能开关	
素材仓库	https://github.com/LBEILC/RhineLabUI	3D 场景、开场动画、档案阵列、阅读面板源码	
dsh 客户端规范	dsh 仓库 packages/client/AGENTS.md（读 v0.1.2-rc.1 tag 的版本；**该文件不在 npm 安装包内，须先 clone dsh 源码仓库**）	slot 注册 API、PropsRuntime；已实证 slot 运行时以 seed 模块注入（`require('@deepseek-ai/dsh-client-ui-slots')` 可用），但 npm 类型包仅到 0.0.1-rc.1 且未落盘，类型开发须自备 d.ts	
dsh 版本发布页	https://github.com/deepseek-ai/deepseek-harness/releases	锚定版本核对与升级跟踪	
dsh 插件注册表	https://dsh.pub/plugins	同类皮肤插件参考	
同主题先例	dsh.pub 上 dsh-theme-rhine-lab 等	仅取莱茵生命配色令牌参考	
P0 第一个动作：git clone 两个参考仓库 + clone dsh 源码仓库并检出 v0.1.2-rc.1 tag，通读上述文件。（两个参考仓库实测均可达：open-sea-skin HEAD=5dd47e7c、RhineLabUI HEAD=c24afa58）
3. 总体架构（R-3：路径职责分离）
浏览器（dsh Web GUI，127.0.0.1:3080，同源）
├── 原生 dsh shell（插件启用时被 <style> 压制为半透明幽灵态）
└── 插件注入层（视觉层：open-sea-skin 纯 DOM 路径）
    ├── 固定层 #rhinelab-ui-root
    │   ├── Three.js canvas（垫底）
    │   └── DOM 交互层（档案卡、阅读面板、输入框）
    ├── <style>（CSS 令牌覆盖 + 原生 UI 透明化）
    └── 数据层（按 P0 决策门结果实现）：
        通道 B（主用）：React 包装组件经 PropsRuntime / ctx.sessions / ctx.remote 取数 → 喂给 rhine-bridge
        兜底 D（B 失败时）：宿主半直读 $DSH_HOME/sessions/**/session.jsonl.zstd → webServer 静态路由 → 客户端 fetch
        注【v1.0-R2 修正】：原"通道 C：dsh-direct-api.js 封装后端 HTTP/WS 端点直连"已删除——实测后端不存在 REST 会话端点，数据只经 `/api` RPC 与 `/api/remote.mux`（Typert Remote），手写该协议与通道 B 属同一传输，不构成独立兜底（出处：SPEC-DEVIATIONS.md D-2）。
核心原则【R-3】：视觉接管与数据获取彻底解耦为两个独立模块，禁止混写。视觉层照抄 open-sea-skin 的**管线**（bundle 门面 + webServer 静态路由 + <style> 注入），**不照抄其视觉架构**——open-sea-skin 是背景+玻璃皮肤而非全屏替换，3D 档案室为新增工作；数据层按决策门结果实现。v0.9 草案中"在 client.js factory 内经 require/ctx services 获取会话服务"的模糊表述删除。
4. P0：三探针实验（R-1、R-2 + 版本核对项，v1.0-R2 修正）
P0 是全项目生死线，耗时约 1~2 天，出口即决策门。
探针 A：视觉接管（已验证路径，照抄管线）
// 全屏容器 + <style> 压制原生 UI + 重启刷新后原生恢复
// 验收：页面全屏被插件覆盖，remove 后原生界面完整恢复
// 已实证的抄写落点（出处 SPEC-DEVIATIONS.md D-3）：
//   oss/plugin/client.js:2  window.__ModuleLoader__.load({ id, factory })  ← 客户端 bundle 门面
//   oss/plugin/index.js:74  ctx.webServer.register({ kind:'prefix', path, handler })  ← 静态资产路由（即 R-6 的答案）
//   oss/cordis.patch.yml:4  - insert: - id: <name>  ← 不改 dsh 源码的挂载方式
//   禁止采用 open-sea-skin 的 harness-plugin/ 路径：它依赖上游不存在的 shell.background 槽位（违反硬约束 1）
探针 B：数据-官方通道【R-1 + v1.0-R2 版本核对项】
先读 v0.1.2-rc.1 tag 的 AGENTS.md（须 clone dsh 源码仓库，npm 包内无此文件）——以当前版本源码为准，不依赖任何旧文档印象。
尝试 ctx.slots.register 注册最小组件，useEffect 中 console.log(useSessions())——控制台出现真实会话数组即通过。
顺带验证：require 能否加载 React【R-B3】——已实证：React 是 shell 的 seed 模块，可 require（出处 D-8）。
版本核对项（v1.0-R2 调整）：ctx.agent 移除与 Inbox 变更属宿主侧 Agent API，与本客户端插件无交集，降为"仅记录"；会话格式 V3 仅在采用兜底 D（直读 session.jsonl.zstd）时升级为 P2 前置必核对项。
已实证的取数面（出处 D-2/D-7）：ctx.remote.session.{list,page,prompt,follow,search,create,cancel,fork,modelCatalog,...}，以及客户端 ctx.sessions.list（useSessions 标准 feed）。
探针 C：数据-兜底通道（v1.0-R2 重定义）
原探针 C（同源 fetch REST 端点）已作废：后端无 REST 会话端点，仅有 /api RPC 与 /api/remote.mux（出处 D-2）。
新探针 C：宿主半注册 ctx.webServer.register({kind:'prefix'}) 路由，读取并解析一份真实 session.jsonl.zstd，客户端同源 fetch 该路由并 console.log 解析结果。
注意鉴权 fence：dsh web 以进程启动令牌换签名 cookie，/api 另需同源校验（curl 直连 3080 返回 401）；页面内 fetch 自动带 cookie，故可行。
P0 出口：数据通道决策门【R-2 修正】
探针 B 通过 ──────────→ 主用官方通道（ctx.remote / ctx.sessions），抗漂移优先
B 失败、C 通过 ────────→ 主用兜底 D（宿主半直读会话日志），与传输层解耦
B、C 均失败 ──────────→ 停止，交付降级为氛围模式，显式报告
同时踩掉插件注册坑【R-7.1，机制已定位】：dsh plugin add 后 reconcilePlugins 会自动把带 dsh.bundle 的依赖写入 profile 的 dsh.profile.bundles；若再手写同 id 的 cordis.patch.yml insert 即双挂载。规避方法：手动维护 cordis.patch.yml，装完后检查配置无重复条目。
新增前置【R-12】：git 来源插件首次安装会因 pnpm 拦截 prepare 构建脚本而失败，须先在 profile 的 pnpm-workspace.yaml 写入 allowBuilds 条目（出处 D-4）。
5. P1~P4 阶段（R-4、R-6、R-10 并入）
阶段	任务	出口条件	
P1 3D 嵌入（第 2 周）	RhineLabUI Vite UMD 构建并入 rhine-dist/；字体子集化、GLB Draco 压缩；【R-6 已实证】资产服务规则＝宿主半 ctx.webServer.register({kind:'prefix', path, handler}) 挂同源静态路由，客户端按前缀 fetch；资产基路径改运行时注入；令牌覆盖使原生 UI 半透明	开场动画完整播放，档案阵列可翻阅（静态数据）；DPR/FPS/reduced-motion 开关可用	
P2 数据桥（第 3 周）	按决策门所选通道实现 shared/rhine-bridge.js；前置【R-10】：克隆 RhineLabUI 后先核对 5×8 结构与阅读面板是否 DOM 层，不符则调整映射表再开工；以 ctx.remote.session.list/page/follow（或兜底 D 直读日志）填充 40 槽位（映射见第 6 节）；标题回退链 title → displayTitle → cwd 基名 → sessionId；"收藏"为插件本地持久化（dsh 无收藏字段）	阵列显示真实 dsh 会话；点击档案卡加载真实历史；新建会话刷新后出现	
P3 输入与流式（第 4 周）	前置【R-4】：发消息决策门——优先级链：chain slot 注册（先读 v0.1.2-rc.1 源码确认插件可注册）→ ctx.remote.session.prompt（已实证存在）→ 兜底 D 的宿主半转发，5 分钟确认可行性后再开发；流式回复打字机追加（session.follow）；工具调用触发统一拆解循环动画 + 参数文本条	3D 界面完成一次完整对话（输入→流式回复→含一次工具调用）；流式中断/重连不卡死	
P4 打磨发布（第 5 周）	检索/收藏/导出接线；模型查看器；README（含锚定版本声明）；npm 发布	第 8 节双列验收全通过	
6. 交互映射与数据契约（已定决策，直接执行）
6.1 40 槽位 ↔ 会话分组（R-10 允许按实际结构微调）
类别	分组	
一	收藏	
二	最近 24h	
三	本周	
四	本月	
五	更早	
每桶最多 8 个，超出滚动；不足时空槽渲染"待归档"占位。
6.2 rhine-bridge 契约
const rhineApi = {
  renderSessionsList(sessions),   // 渲染 40 槽位
  onArchiveSelected(archiveId),
  renderMessageStream(chunk),
  onMessageSubmitted(text),
  onSearchQuery(keyword),
  onToggleFavorite(archiveId),
  triggerToolAnim(toolName, params),
}
dsh 侧数据来源：通道 B（React 组件经 PropsRuntime / ctx.sessions / ctx.remote 取数喂入）或兜底 D（宿主半直读会话日志经 webServer 路由）。工具动画采用统一拆解循环 + 文本条方案（无语义映射，100% 可实现）。【v1.0-R2】原"通道 C（dsh-direct-api.js 封装端点）"作废；"检索"受 session/search 上限约束（20 条/次、snippet ≤240 码点），"导出"无 API，需由 session/page 记录自行拼装。
7. 修订日志（16 条完整表）
编号	修订内容	依据	影响章节	引入版本	
R-1	P0 重构为三探针实验，替换原红色 div 单一实验	A3：风险排序颠倒，原 P0 只测已验证的视觉路径	§4	v1.0-R1	
R-2	新增 P0 出口"数据通道决策门"（B/C/停止三选一）	A1：架构混用需显式裁决	§4	v1.0-R1（v1.0-R2 修正：C 与 B 同传输，改为"B 主用 / 兜底 D 直读日志 / 停止"）
R-3	视觉层与数据层职责分离，禁止混写；删除"factory 内经 require 获取服务"模糊表述	A1：两条互斥机制被混进同一方案	§3	v1.0-R1	
R-4	发消息通道改为 P3 前置决策门（chain slot → 消息 API → 直连端点）	A2：发消息是核心体验却全部建立在未验证假设上	§5 P3	v1.0-R1	
R-5	定义构建管线：esbuild 打包 src/client-entry.js → plugin/client.js；RhineLabUI 独立 Vite UMD	B1：管线未定义，模板与本项目结构不同	§5 P1	v1.0-R1	
R-6	P1 新增资产服务验证：读 open-sea-skin 资产引用方式；基路径改运行时注入	B2：插件静态资产 URL 规则未验证，P1 易踩 404	§5 P1	v1.0-R1	
R-7	风险表增补两条：__ModuleLoader__ 为疑似内部 API（特性检测缺失时静默不启用 + console.warn）；React 可用性仅路径乙需要	B3、B4：风险清单不完整	§9 风险	v1.0-R1	
R-8	验收清单拆分"代理自验"（可写断言脚本）与"人工验收"（看浏览器）两列	C2：原清单未区分自验/人验	§8	v1.0-R1	
R-9	版本锚定策略具体化：起步锁 v0.1.5-alpha.1（升级为最新，因 v0.1.5 修复长会话卡顿与 Windows 兼容）	C4 + 2026-09-08 releases 核查：v0.1.5 含会话 V3、Agent/Inbox 破坏性变更	§1 硬约束 5	v1.0-R1（v1.0-R2 修正：实测 dist-tags 为 latest=0.1.2-rc.1、alpha=0.1.5-alpha.1，改锚 0.1.2-rc.1）
R-10	P2 前置"RhineLabUI 结构核对"：克隆后先核对 5×8 结构与阅读面板 DOM 层，不符调整映射再开工	C1：原方案直接采用计划书数字未核对源码	§5 P2	v1.0-R1	
R-11	持久化痕迹规则：localStorage 键一律 rhinelab-ui: 前缀；"来去无痕"定义为 UI 状态零残留，用户数据属合法持久	C3：范围未明确，代理可能过度清理或过度残留	§1 硬约束 3	v1.0-R1	
R-12	git 来源插件首次安装必失败：pnpm 拦截 prepare 构建脚本，须先在 profile 的 pnpm-workspace.yaml 写 allowBuilds	dsh CLI 自身提示（lib/plugin-F7ZVfRyo.js:125）	§4 P0 前置、§8 验收	v1.0-R2	
R-13	P0 前置补 clone dsh 源码仓库：npm 安装包只含 lib/，无 packages/client/AGENTS.md	实测安装目录结构	§2、§4 探针 B	v1.0-R2	
R-14	数据契约缺口登记：title 可选需回退链；dsh 无收藏字段（改插件本地持久化）；无导出 API；search 上限 20 条/240 码点	session-controller 类型定义（types.d.ts:138/233/427、client/sessions/service.d.ts:35）	§5 P2、§6.1/§6.2	v1.0-R2	
R-15	视觉层照抄边界收敛为"管线"，并禁用 open-sea-skin 的 harness-plugin/ 路径（依赖上游不存在的 shell.background 槽位，违反硬约束 1）	open-sea-skin README 自述 + 0.1.2-rc.1/0.1.5-alpha.1 两版 grep 均无该槽位	§3、§5 P1	v1.0-R2	
R-16	鉴权 fence 登记：/api 与 WS 升级需签名 cookie + 同源校验（curl 直连 3080 返回 401）；页面内 fetch 自动满足，但手写通道必须遵守	client-connection 源码（index.js:178/199/356）+ curl 实测	§4 探针 C、§9 风险	v1.0-R2	
本轮基于 dsh releases 新增的补充（v1.0-R2 调整）：原"探针 B 专项核对 v0.1.5 的 ctx.agent 移除、Inbox API 变更"降级为"仅记录"（属宿主侧，与本客户端插件无交集）；会话格式 V3 仅在采用兜底 D（宿主半直读 session.jsonl.zstd）时升级为 P2 前置必核对项。全部实证出处见 SPEC-DEVIATIONS.md。
8. 最终验收清单（R-8 双列）
代理自验（可写断言脚本，自动化）
插件 add/remove 循环通过，cordis.patch.yml 无重复条目
控制台零报错、canvas 尺寸非零
会话数组成功渲染进 40 槽位
流式 chunk 到达率正常
卸载后 DOM/style 零残留（rhinelab-ui: 前缀键除外，属合法持久）
人工验收（看浏览器）
dsh plugin --profile web add 'github:<repo>#v0.1.0' → 重启 → 刷新 → 开场动画完整播放（逐字、标志绘制、身份接入、权限扫描、欢迎转场、档案展开）【R-12：首次安装前须先在 profile 的 pnpm-workspace.yaml 写 allowBuilds，否则 pnpm 拦截构建脚本导致 add 失败】
40 槽位显示真实会话，上下翻阅、左右切列循环
点击档案卡 → 3D 升起 → 镜头衔接 → 阅读面板显示历史
3D 场景输入 → 流式打字回复
工具调用 → 拆解动画 + 参数文本条
检索/收藏/导出可用
模型查看器 360° 环绕、六组拆解重组可用
dsh plugin --profile web remove rhinelab-ui → 重启 → 刷新 → 原生界面与全部功能完整恢复，无残留
第 8 条是本项目与 dsh 契约的底线：进得去，出得来，来去无痕。
9. 风险与回退（R-7 增补后完整版）
风险	对策	
dsh 预览期 API 漂移（已实证：0.1.5 移除 ctx.agent、改 Inbox；客户端侧 session/* 两版零差异）	锚定 v0.1.2-rc.1；所有 dsh 接口调用收拢在单文件；升级前先跑探针 B 回归	
dsh plugin add 双重注册【R-7.1，机制已定位】	机制＝reconcilePlugins 自动写入 dsh.profile.bundles ＋ 手写同 id 的 cordis.patch.yml insert；对策：手动维护 cordis.patch.yml，装后检查无重复	
git 来源插件安装被 pnpm 拦截构建脚本【R-12】	安装前在 profile 的 pnpm-workspace.yaml 预置 allowBuilds 条目	
/api 与 WS 升级的鉴权 fence【R-16】	页面内 fetch/WebSocket 自动携带签名 cookie 与同源标记，勿在页面外或裸 fetch 中假设匿名可访问	
字体 19.7MB 拖慢首屏	P1 强制子集化；GLB Draco 压缩；大资产走 webServer 静态路由（勿打进 client.js），参考 open-sea-skin 的静态 vendor + iframe 模式	
Three.js 性能	复用 RhineLabUI 降级开关 + DPR cap + FPS 上限 + prefers-reduced-motion	
全屏接管被 dsh 更新破坏	P0 探针 A 前置验证；官方备选路径＝注册 shell.overlay 槽位（已实证存在且被 AppFrame 渲染）；最坏回退 open-sea-skin 共存模式（视觉降级、功能完整）	
__ModuleLoader__ 疑似内部 API【R-7】	版本锁定 + 启动特性检测，缺失时静默不启用 + console.warn	
React 可用性【R-7，已实证】	已确认 React 为 shell seed 模块，可 require；不再作为失败分支	
主用通道与兜底 D 均失败【R-2 修正】	停止，交付氛围模式，显式报告后重规划	
总回退路径：若 P2/P3 数据桥受阻，降级交付"P1 状态 + 原生 UI 半透明可交互"——open-sea-skin 的完整等价物，仍是合格的莱茵生命 3D 皮肤。
版本落款：v1.0-R2 · 2026-09-09 · 锚定 dsh v0.1.2-rc.1（npm latest）· 取代 v1.0-R1 与 v0.9 草案 · R-1~R-16 全部生效 · 实证出处见 SPEC-DEVIATIONS.md。
待办（P0 前，按用户裁决 2026-09-09）：① 在 $DSH_HOME/profiles/web/pnpm-workspace.yaml 预置 allowBuilds；② clone dsh 源码仓库并检出 v0.1.2-rc.1 tag 以读 packages/client/AGENTS.md。P0 三探针待用户确认后开跑。
